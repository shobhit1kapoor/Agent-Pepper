# Optional: enables live Anakin URL Scraper research. Without it, scoped Playwright fallback still works.
ANAKIN_API_KEY=

# Optional: enables read-only status discovery for an operator-started local BrowserOS server.
# The endpoint must be http://127.0.0.1:<port> or http://localhost:<port>.
BROWSEROS_ENDPOINT=
